#include<stdio.h>
int main(){
    double a[]={10,20,30,40,50}, sum=0, min=1e9, max=-1e9;
    int n=5;
    for(int i=0;i<n;i++){ sum+=a[i]; if(a[i]<min)min=a[i]; if(a[i]>max)max=a[i]; }
    printf("Trimmed average = %f\n", (sum-min-max)/(n-2));
    return 0;
}
