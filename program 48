#include<stdio.h>
int main(){
    double arr[]={-2,5,7,-1,10}, sum=0; int count=0;
    for(int i=0;i<5;i++) if(arr[i]>0){ sum+=arr[i]; count++; }
    printf("Avg positives = %f\n", count?sum/count:0.0);
    return 0;
}
