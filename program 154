#include <stdio.h>
int main() {
    int n=7, prime=1;
    for(int i=2;i<=n/2;i++)
        if(n%i==0) { prime=0; break; }
    if(prime) printf("%d is Prime", n);
    else printf("%d is Not Prime", n);
    return 0;
}
