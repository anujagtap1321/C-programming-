#include<stdio.h>
int main(){
    int marks[]={45,60,30,80,90}, n=5, pass=40, count=0;
    for(int i=0;i<n;i++) if(marks[i]>=pass) count++;
    printf("Pass percentage = %f%%\n", (count*100.0)/n);
    return 0;
}
