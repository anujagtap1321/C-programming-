#include<stdio.h>
#include<string.h>
int main(){
    char s[]="hello world";
    int n=strlen(s), v=0;
    for(int i=0;i<n;i++){ char c=s[i]; if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u') v++; }
    printf("Vowel percentage = %f%%\n", v*100.0/n);
    return 0;
}
