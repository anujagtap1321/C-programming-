#include<stdio.h>
int main(){
    int n;
    printf("Enter n: ");
    if(scanf("%d",&n)!=1) return 0;
    double x, sum=0;
    for(int i=0;i<n;i++){ scanf("%lf",&x); sum+=x; }
    printf("Average = %f\n", sum/n);
    return 0;
}
